

  <h1 class= "text-center mt-4">das sind meinde Indexirungen</h1>

  <div class="container">
    <% @articles.each do |article| %>
    <div class="row justify-content-md-center">
        <div class="col-8 mt-4">
          <div class="card text-center shadow p-1 mb-5 bg-white">
            <div class="card-header">
              von Wonwvonf
            </div>
            <div class="card-body">
              <h5 class="card-title"> <td><%= link_to article.title, article_path(article), class:"text-success" %></td></h5>
              <p class="card-text"><%= truncate(article.description, length:100) %></p>
              <%= link_to "Zeigen", article_path(article), class: "btn btn-outline-success" %>
              <%= link_to "Bearbeiten", edit_article_path(article), class: "btn btn-outline-secondary" %>
              <%= link_to "Löschen", article_path(article), class: "btn btn-outline-danger" %>

            </div>
            <div class="card-footer text-muted">
              <small> Created <% time_ago_in_words(article.created_at) %> ago, edited <%= time_ago_in_words(article.updated_at) %> ago</small>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>




