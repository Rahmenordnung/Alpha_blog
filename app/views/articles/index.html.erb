<h1>das sind meinde Indexirungen</h1>
<table>
  <thead>
    <tr>
      <th>Titel</th>
      <th>Beschreibungen</th>
      <th colspan="3">Actionen</th>


    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.description %></td>
        <td><%= link_to 'Zeigen' , article_path(article)  %></td>
        <td><%= link_to 'Bearbeiten' , edit_article_path(article)  %></td>
        <td><%= link_to 'Löschen' , article_path(article), method: :delete , data: {confirm: "Bist du sicher, oder?"}%></td>
      </tr>
      <% end %>
  </tbody>

</table>

<p></p>
<%= link_to 'Create new article' , new_article_path %>